Пример вёрстки с использованием шаблонизатора pug, scss и сборкой gulp

Установка:

`npm i` 

Запуск:

`gulp` - для разработки

`gulp optiimg` - для оптимизации изображений,при необходимости

`gulp uploads` - для тяжёлых ресурсов, при необходимости
